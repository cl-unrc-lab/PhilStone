For downloading the docker:

docker pull aguirre/sketch-frontend-docker

For running the docker:

docker run -it aguirre/sketch-frontend-docker

For running the command you must execute:

docker exec [CONTAINER NAME] psketch
